package com.gymlog.app.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta "Hunter" (Dark Mode profundo + Neón Azul/Morado)

val HunterBlack = Color(0xFF050910) // Fondo principal
val HunterDarkBlue = Color(0xFF0F1623) // Fondo tarjetas
val HunterSurface = Color(0xFF1A2233) // Superficies

// COLORES PRINCIPALES
val HunterPrimary = Color(0xFF2E8FFF) // Azul Eléctrico (Información, Selección)
val HunterPurple = Color(0xFFD500F9) // Morado Neón (Acción Principal, Progreso)
val HunterCyan = Color(0xFF00E5FF) // Cian (Completado, Éxito)

// Acentos
val HunterSecondary = Color(0xFFFF3D00) // Naranja (Solo para errores críticos/borrar)
val HunterTextPrimary = Color(0xFFFFFFFF)
val HunterTextSecondary = Color(0xFF94A3B8)

// Rangos (Reajustados a gama fría/azulada/blanca para no romper estética)
val RankE = Color(0xFFB0BEC5) // Gris
val RankD = Color(0xFF90CAF9) // Azul muy claro
val RankC = Color(0xFF4FC3F7) // Azul claro
val RankB = Color(0xFF2E8FFF) // Azul intenso
val RankA = Color(0xFFD500F9) // Morado
val RankS = Color(0xFFFFFFFF) // Blanco brillante